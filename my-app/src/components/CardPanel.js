import React from "react";
import Card from '@material-ui/core/Card';

const CardPanel = ({ onIncrementAsync, onDecrementAsync, value }) => (
    <div>
        <Card></Card>

    </div>
);
export default CardPanel;